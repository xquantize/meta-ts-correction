% Copy this block to the TOP of sections/run_log.tex after a useful run.
% Fill every field. Then update sections/results.tex if the number is paper-worthy.
% Bump the R# (newest = highest).

\subsection*{R\# --- Short title (YYYY-MM-DD)}
\begin{itemize}
  \item \textbf{Config:} \texttt{configs/....yaml}
  \item \textbf{Run id:} \texttt{PASTE\_FULL\_RUN\_ID}
  \item \textbf{Setup:} dataset / split / model / key knobs
  \item \textbf{Numbers:} MASE / sMAPE / $\Delta$ / $p$-values as relevant
  \item \textbf{Decision:} go / no\_go / n/a
  \item \textbf{Paper note:} one sentence on why this matters for the write-up
\end{itemize}
