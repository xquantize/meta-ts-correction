% Chronological experiment log. Append newest entries at the TOP.
% Rule: every number that might enter the paper must appear here with a full run_id.

\subsection*{R8 --- Multi-seed rule-search sweep (2026-07-31)}
\begin{itemize}
  \item \textbf{Config:} \texttt{configs/seed\_sweep\_chronos\_m4\_hourly.yaml}
  \item \textbf{Artifact:}
        \texttt{outputs/tables/seed\_sweep/seed\_sweep\_chronos\_m4\_hourly/}
  \item \textbf{Setup:} seeds $\{0,1,2,3,4\}$; for each seed, train/reuse
        corrector v1 and v2 (series split + training seed), then R7-style
        val-selected abstain rule scored once on test. Seed 0 reuses R3/R4.
        CLI override \texttt{meta-ts-run \ldots{} --seed N} freezes the effective
        seed into \texttt{config.yaml} / manifest.
  \item \textbf{Numbers (v1, 5 seeds):} beats base $4/5$; significant
        ($p<0.05$) $3/5$; mean test margin $+0.0057\pm0.0057$.
        Selected features unstable:
        \{\texttt{seasonal\_corr}:2, \texttt{trend\_corr}:2,
        \texttt{abs\_diff\_mean}:1\}.
  \item \textbf{Numbers (v2, 5 seeds):} beats base $5/5$; significant $2/5$;
        mean test margin $+0.0065\pm0.0050$.
        Feature stable: \texttt{abs\_diff\_mean} on $4/5$ seeds
        (quantiles $0.5$--$0.8$); one seed picked \texttt{cv}.
  \item \textbf{Decision:} the \emph{direction} of the effect is robust for v2
        (always beats base on mean); \emph{significance} is not (only $2/5$).
        Feature choice for v2 is mostly stable; for v1 it is not.
  \item \textbf{Caveats:} test-fold Chronos MASE varies by seed
        ($0.78$--$1.07$) because the held-out series change; still M4/Chronos
        leakage; 61-candidate selection per seed without multiplicity correction.
  \item \textbf{Paper note:} R7 was not a one-seed fluke for mean improvement,
        but $p$-values alone would overclaim. Prefer reporting win rates /
        margins across seeds.
\end{itemize}

\subsection*{R7 --- Val-selected abstain rule (2026-07-31)}
\begin{itemize}
  \item \textbf{Config:} \texttt{configs/rule\_search\_chronos\_m4\_hourly.yaml}
  \item \textbf{Artifact:}
        \texttt{outputs/tables/rule\_search/rule\_search\_chronos\_m4\_hourly/}
  \item \textbf{Source runs:} R3 / R4 (same ids as R5--R6).
  \item \textbf{Why:} R6 chose \texttt{abs\_diff\_mean} \emph{after} looking at
        test-fold strata (R5). Here the rule is chosen on the \textbf{val} fold
        only; thresholds are still fit on train meta-features; test is scored once.
        Val-fold scores are obtained by replaying \texttt{model.pt} (runs only
        persist test scores).
  \item \textbf{Search:} 61 candidates $=$ \{\texttt{abs\_diff\_mean},
        \texttt{cv}, \texttt{seasonal\_corr}, \texttt{trend\_corr},
        \texttt{n\_train}\} $\times$ 6 quantiles $\times$ \{high, low\},
        plus an explicit \emph{never apply} policy.
  \item \textbf{Numbers (v1):} selected \texttt{seasonal\_corr} high Q0.5;
        val $0.9112$ vs val base $0.9454$; test base $1.0700$, always-corr
        $1.0691$, selective $\mathbf{1.0692}$ ($44\%$ applied),
        Wilcoxon $p=0.0747$ --- beats base, \emph{not} significant.
  \item \textbf{Numbers (v2):} selected \texttt{abs\_diff\_mean} high Q0.6
        (threshold $34.87$); val $0.9204$ vs base $0.9454$; test base $1.0700$,
        always-corr $2.1557$, selective $\mathbf{1.0554}$ ($35\%$ applied),
        Wilcoxon $p=0.0267$ --- beats base and significant.
  \item \textbf{Decision:} R6 survives honest selection for v2; v1 does not.
        \emph{never apply} ranked 7/61 on val for v2 and 48/61 for v1.
  \item \textbf{Caveats:} 61 candidates selected on $n=62$ val series (selection
        variance, no multiplicity correction); val margin ($-0.034$) is larger
        than the test margin ($-0.015$); M4 leakage still applies.
  \item \textbf{Paper note:} the abstain result is not an artifact of peeking ---
        the gate story is worth building, but only v2 clears $p<0.05$.
\end{itemize}

\subsection*{R6 --- Selective apply replay (2026-07-29)}
\begin{itemize}
  \item \textbf{Config:} \texttt{configs/selective\_apply\_chronos\_m4\_hourly.yaml}
  \item \textbf{Artifact:}
        \texttt{outputs/tables/selective\_apply/selective\_apply\_chronos\_m4\_hourly/}
  \item \textbf{Source runs:} R3 / R4 (same ids as R5).
  \item \textbf{Setup:} no retrain. Fit \texttt{abs\_diff\_mean} threshold on
        \emph{train} fold (quantile $0.75$ $\Rightarrow$ threshold $103.984$);
        on test, use corrected series MASE iff feature $\ge$ threshold, else
        keep Chronos. Applied on $16/62$ series ($26\%$).
  \item \textbf{Numbers (mean test MASE):}
        \begin{itemize}
          \item v1: base $1.070$ / always-corr $1.069$ / selective $1.067$;
                Wilcoxon $p=0.060$ (ties dropped; $n=16$ applied).
          \item v2: base $1.070$ / always-corr $2.156$ / selective $1.053$;
                Wilcoxon $p=0.0049$ ($n=16$).
        \end{itemize}
  \item \textbf{Decision:} \texttt{worth\_gate=true} for both (selective beats
        always-corrected and is no worse than base on mean MASE).
  \item \textbf{Paper note:} a hard abstain rule already rescues v2; motivates a
        learned $\gamma$ gate as the next \emph{method} commit.
\end{itemize}

\subsection*{R5 --- When-it-helps stratification (2026-07-29)}
\begin{itemize}
  \item \textbf{Config:} \texttt{configs/when\_it\_helps\_chronos\_m4\_hourly.yaml}
  \item \textbf{Artifact:}
        \texttt{outputs/tables/when\_it\_helps/when\_it\_helps\_chronos\_m4\_hourly/}
  \item \textbf{Source runs:} R3
        (\texttt{20260728T041734Z\_corrector\_v1\_chronos\_m4\_hourly\_d9e83543342c}),
        R4
        (\texttt{20260728T214721Z\_corrector\_v2\_chronos\_m4\_hourly\_de857d53af29}).
  \item \textbf{Setup:} test fold $n=62$; descriptive quartile bins on the same
        fold (not a pre-registered policy); features
        \{\texttt{cv}, \texttt{trend\_corr}, \texttt{seasonal\_corr},
        \texttt{abs\_diff\_mean}, \texttt{base\_mase}\}
        (\texttt{n\_train} collapsed / skipped on this fold).
  \item \textbf{Numbers (overall):} v1 frac helped $=0.500$, mean $\Delta$MASE
        $=+0.0009$; v2 frac helped $=0.403$, mean $\Delta$MASE $=-1.086$.
  \item \textbf{Numbers (highlights):} v1 mild heterogeneity --- mid
        \texttt{trend\_corr} quartile mean $\Delta=+0.104$ / frac helped
        $0.667$ vs low-trend $\Delta=-0.114$ / $0.312$.
        v2 damage concentrated --- high \texttt{seasonal\_corr} $\Delta=-3.793$
        / helped $0.188$; low \texttt{abs\_diff\_mean} $\Delta=-3.899$ / $0.125$;
        only high \texttt{abs\_diff\_mean} quartile is net-positive
        ($\Delta=+0.067$, helped $0.750$).
  \item \textbf{Decision:} analysis only (supports gate / selective apply).
  \item \textbf{Paper note:} correction is not uniformly useless or harmful;
        a gate (or abstain rule) is the natural next method step.
\end{itemize}

\subsection*{R4 --- Corrector v2 + meta-features (2026-07-29)}
\begin{itemize}
  \item \textbf{Config:} \texttt{configs/corrector\_v2\_chronos\_m4\_hourly.yaml}
  \item \textbf{Run id:}
        \texttt{20260728T214721Z\_corrector\_v2\_chronos\_m4\_hourly\_de857d53af29}
  \item \textbf{Setup:} same split seed 0 as v1 (test $n=62$);
        features \{\texttt{y\_pred}, \texttt{step\_frac}, \texttt{cv},
        \texttt{trend\_corr}, \texttt{seasonal\_corr}, \texttt{abs\_diff\_mean},
        \texttt{log\_n\_train}\}; 1345 params; no gate.
  \item \textbf{Numbers:} Chronos MASE 1.069970 / sMAPE 11.324201;
        corrected MASE 2.155677 / sMAPE 12.508070;
        $\Delta$MASE $= -1.085707$ (worse); Wilcoxon $p=0.998$ ($n=62$).
  \item \textbf{Decision:} \textbf{no\_go}.
  \item \textbf{Paper note:} naive meta-conditioning without a gate can
        \emph{hurt}; motivates gating and when-it-helps stratification.
\end{itemize}

\subsection*{R3 --- Corrector v1 (2026-07-28)}
\begin{itemize}
  \item \textbf{Config:} \texttt{configs/corrector\_v1\_chronos\_m4\_hourly.yaml}
  \item \textbf{Run id:}
        \texttt{20260728T041734Z\_corrector\_v1\_chronos\_m4\_hourly\_d9e83543342c}
  \item \textbf{Setup:} series split 70/15/15, seed 0, test $n=62$;
        features \{\texttt{y\_pred}, \texttt{step\_frac}\}; 1185 params;
        no meta, no gate. Residuals from
        \texttt{residuals\_chronos\_m4\_hourly}.
  \item \textbf{Numbers:} Chronos MASE 1.069970 / sMAPE 11.324201;
        corrected MASE 1.069064 / sMAPE 11.304499;
        $\Delta$MASE $= 0.000906$; Wilcoxon $p=0.221329$ ($n=62$).
  \item \textbf{Decision:} \textbf{no\_go} (pre-committed rule).
  \item \textbf{Paper note:} point residual alone is not enough on this split.
\end{itemize}

\subsection*{R2 --- Chronos-Bolt tiny, full Hourly (2026-07-28)}
\begin{itemize}
  \item \textbf{Config:} \texttt{configs/chronos\_bolt\_tiny\_m4\_hourly.yaml}
  \item \textbf{Run id:}
        \texttt{20260728T015619Z\_chronos\_bolt\_tiny\_m4\_hourly\_f46d89465a4a}
  \item \textbf{Setup:} M4 Hourly full set, $n=414$, frozen zero-shot (MPS).
  \item \textbf{Numbers:} MASE 0.931253, sMAPE 8.974144.
  \item \textbf{Paper note:} beats seasonal naive; source forecasts for the
        residual dataset.
\end{itemize}

\subsection*{R1 --- Seasonal naive, full Hourly (2026-07-27)}
\begin{itemize}
  \item \textbf{Config:} \texttt{configs/seasonal\_naive\_m4\_hourly.yaml}
  \item \textbf{Run id:}
        \texttt{20260727T032226Z\_seasonal\_naive\_m4\_hourly\_087ea3747348}
  \item \textbf{Setup:} M4 Hourly full set, $n=414$.
  \item \textbf{Numbers:} MASE 1.193210, sMAPE 13.912273.
  \item \textbf{Paper note:} baseline floor for the suite.
\end{itemize}
