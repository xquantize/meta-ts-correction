\begin{enumerate}
  \item Gated correction ($\gamma$ shrinks updates when the base model is already
        good) --- can it undo the v2 degradation?
  \item Stratified ``when it helps'' analysis on meta-features already stored
        with residuals.
  \item Clean / non-leaked eval for any strong claim (M4 is high leakage risk
        for Chronos).
\end{enumerate}
