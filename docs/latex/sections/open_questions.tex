\begin{enumerate}
  \item Learned $\gamma$ gate (or hard gate) inside the corrector --- can it
        match the R7 val-selected abstain rule without a rule grid?
  \item Selection variance: R7 ranks 61 candidates on $n=62$ val series with no
        multiplicity correction. Repeat over seeds / splits before trusting the
        margin.
  \item Clean / non-leaked eval for any strong claim (M4 is high leakage risk
        for Chronos).
\end{enumerate}
